#include <stdio.h>
/*
int main()
{
  	int a = 6;					// create a variables
  	int i = 1;
  	int b = 0;
  		for (i = 1; i < a; i++) {		// create a cycle "for" to find divisors
    		if (a % i == 0 ){
      		b = b + i;
    		}
  		}
  	if (a == b) {					// set a condition for to get the result
    	printf( "The number is perfect");
  	}
  	else {
    	printf( "The number is not perfect");
  	}
  return 0;
 } */
 /*
  int main(){
	int a = 6;					// create a variables
	int i = 0;
	int b = 0;  
  		do {					// create a cycle "do...while" to find divisors
  		i++;
  		if (a % i == 0 ){
      		b = b + i;
    		}
      		} while ( i-1 < a);
     		b = b - a;
     	if (a == b){					// set a condition for to get the result
    	printf( "The number is perfect");
  	}
  	else {
    	printf( "The number is not perfect");
  	}
  return 0;
  } */
  /*
  int main(){
  	int a = 5;					// create a variables
  	int i = 1;
  	int b = 0;
  		while ( i < a ){			// create a cycle "while" to find divisors
  		if ( a % i == 0){
  		b = b + i;
  		}
  		i++;
  		}
  		if ( a == b ){				// set a condition for to get the result
  		printf ("The number is perfect");
  		} else {
  		printf ("The number is not perfect");
  		}
  return 0;
  } */
